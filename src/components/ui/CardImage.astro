---
import type { CardImage } from "@/types";

const { image } = Astro.props as CardImage;

const bgClass = `bg-[url('${image.mobile}')] md:bg-[url('${image.desktop}')]`;
---

<div class={`${bgClass} bg-cover bg-no-repeat bg-center h-[240px] md:h-[340px] lg:h-[480px]`} />